export type VideoData = {
    duration: number;
    time_start: number;
    id: string;
    is_live: boolean;
    was_live: boolean;
    manifest_url: string;
    title: string;
    description: string;
    thumbnail: string;
};
