export type VideoData = {
    duration: number;
    timeStart: number;
    id: string;
    isLive: boolean;
    wasLive: boolean;
    manifestUrl: string;
    title: string;
    description: string;
    thumbnail: string;
};
